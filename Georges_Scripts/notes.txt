sudo apt install fping
snmpd.conf

make time command to set right time

make validation/check services script
make resize disk script




fix TARGET_IP in honeypot script to use any ip rather than local host
	have to incorperate adding a second ip to the network adaptor



CREATE TABLE honeypot (
    id SERIAL PRIMARY KEY, -- Optional: Unique identifier for each row
    timestamp TIMESTAMP NOT NULL,
    alert_type VARCHAR(255) NOT NULL,
    src_ip INET NOT NULL,
    dst_ip INET NOT NULL,
    port INT NOT NULL
);


CREATE TABLE suricata (
    id SERIAL PRIMARY KEY, -- Optional: Unique identifier for each row
    timestamp TIMESTAMP NOT NULL,
    source_ip INET NOT NULL,
    source_port INT NOT NULL,
    dest_ip INET NOT NULL,
    dest_port INT NOT NULL,
    protocol VARCHAR(50) NOT NULL,
    alert_message TEXT NOT NULL
);
